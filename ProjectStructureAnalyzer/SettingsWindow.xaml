<Window x:Class="ProjectStructureAnalyzer.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Настройки" Height="400" Width="300" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Фильтры папок:" FontWeight="Bold" Margin="0,0,0,5"/>
        <StackPanel Grid.Row="1" Margin="0,5,0,5">
            <CheckBox x:Name="FolderSrcCheckBox" Content="src" IsChecked="True"/>
            <CheckBox x:Name="FolderBinCheckBox" Content="bin" IsChecked="True"/>
            <CheckBox x:Name="FolderObjCheckBox" Content="obj" IsChecked="True"/>
            <CheckBox x:Name="FolderPropertiesCheckBox" Content="Properties" IsChecked="True"/>
        </StackPanel>
        <TextBlock Text="Фильтры файлов (расширения):" FontWeight="Bold" Grid.Row="2" Margin="0,0,0,5"/>
        <StackPanel Grid.Row="3" Margin="0,5,0,5">
            <CheckBox x:Name="FileCsCheckBox" Content=".cs" IsChecked="True"/>
            <CheckBox x:Name="FileSlnCheckBox" Content=".sln" IsChecked="True"/>
            <CheckBox x:Name="FileCsprojCheckBox" Content=".csproj" IsChecked="True"/>
            <CheckBox x:Name="FileConfigCheckBox" Content=".config" IsChecked="True"/>
        </StackPanel>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="SaveButton" Content="Сохранить" Click="SaveButton_Click" Margin="5" Padding="5"/>
            <Button x:Name="CancelButton" Content="Отмена" Click="CancelButton_Click" Margin="5" Padding="5"/>
        </StackPanel>
    </Grid>
</Window>